{
	"name": "client",
	"license": "MIT",
	"private": true,
	"type": "module",
	"version": "0.0.0",
	"scripts": {
		"dev": "vite",
		"build": "tsc -b && vite build",
		"preview": "vite preview"
	},
	"devDependencies": {
		"server": "workspace:*",
		"definitions": "workspace:*"
	}
}
